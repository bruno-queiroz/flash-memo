const NoDecks = () => {
  return (
    <div className="dark:bg-neutral-900 shadow-md bg-gray-200 px-4 py-8 rounded w-full text-center max-w-[max-content] mx-auto">
      Looks like you don't have a deck yet. Why don't you create one?{" "}
    </div>
  );
};

export default NoDecks;
